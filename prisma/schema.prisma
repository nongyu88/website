generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "debian-openssl-3.0.x"] // <-- Reduced to save 80+ MB!
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  company   String?
  isApproved Boolean  @default(false)
  createdAt DateTime @default(now())
}